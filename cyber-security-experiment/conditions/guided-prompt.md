# Guided condition: model-based incident response

Use the exact C2 contract in `../benchmark/target-challenge.md` and the
`provengo-controller-planner` workflow. Build an open attacker that retains all
legal choices, encode a causal controller using observations only, and add
legality, safety, and bounded-recovery monitors. Formally verify the policy to
the complete finite bound and preserve the command, output, and any
counterexample-driven revisions.

Return the same four artifacts requested from the baseline, plus the executable
model and verification evidence.
