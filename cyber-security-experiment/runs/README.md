# Trial artifacts

Create one immutable directory per trial, for example `baseline-001` or
`guided-001`. Preserve prompt, first response, translated policy, verifier log,
HTML counterexample report, timing/token metadata, and SHA-256 hashes. Never
overwrite an earlier trial during repair.
