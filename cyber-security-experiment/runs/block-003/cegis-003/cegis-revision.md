Provengo rejected the current `controller.js`. The attached counterexample is a
legal trace from the frozen C2 attacker model. Generalize from it, identify the
policy/state error, and replace `controller.js` with the smallest complete repair
that still covers all other legal branches. Do not modify other files or weaken
the environment.

